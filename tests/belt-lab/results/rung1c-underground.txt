Rung 1c — underground PAIR relocate (two-pass: build both ends first, then restore).
Build turbo underground input@100,135 → output@103,135; feed input hoods ~220 ticks to fill tunnel; at t=300
atomic-capture all 4 lines of BOTH ends → rebuild pair offset (y+10) → sorted insert_at restore → rank diff.

RESULT:
  [R1C] srcTotal=128 dstTotal=128
  [R1C] countDrops(0): NONE
  [R1C] maxDelta=0.000000 driftCount=0

CONCLUSION:
- Underground relocate is PERFECT: 128=128, ZERO drops, ZERO position drift. The two-pass (both ends created
  before restore) + sorted insert_at per line is fully lossless AND position-exact for undergrounds.
- (Zero drift here vs 0.039-0.125 on the corner loop: the fed/filled tunnel settled items to exact grid
  positions, so capture+reinsert is identity. Mid-motion off-grid items drift sub-tile but never lose.)
- COMBINED with R1a/R1b: capture(get_detailed_contents) -> two-pass build -> sorted insert_at(pos,bss) is
  PROVEN ITEM-LOSSLESS across single belt, chained corner segment (incl. max density), and undergrounds.
  => The belt RESTORE algorithm is NOT the source of production loss.
  => Suspect shifts to EXPORT (multi-tick scan / does it capture POSITION? / JSON float precision) or to the
     PRODUCTION code deviating from this proven algorithm. Verify production extract_belt_items + belt_restoration.
