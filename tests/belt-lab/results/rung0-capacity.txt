Rung 0 (b)+(c) — exit buffer + capacity (turbo underground input@100,100 → output@103,100)

insert_at_back fill (one back-insert per line per tick — items don't advance within a tick):
  CAPACITY total=32 | e1L1(0.5)=4 e1L2(0.5)=4 e1L3(3)=4 e1L4(3)=4 e2L1(0.5)=4 e2L2(0.5)=4 e2L3(1)=4 e2L4(1)=4
  => insert_at_back only fills the BACK slot (4) once per tick. A belt CANNOT be filled via insert_at_back in
     one tick. RESTORE MUST USE insert_at(position).

insert_at dense fill (step 0.2 across each line_length, stacks of 4):
  DENSE total=176 | e1L1(0.5)=12 e1L2(0.5)=12 e1L3(3)=48 e1L4(3)=48 e2L1(0.5)=8 e2L2(0.5)=8 e2L3(1)=20 e2L4(1)=20
  => entrance tunnel lines e1.L3/L4 hold the bulk (48 each); exit buffer e2.L3/L4 hold real storage (20 each).
     All 8 lines hold items => capture must read all (get_max_transport_line_index covers it).
  (Numbers approximate — 0.2 step doesn't hit exact slot boundaries; note e1L1=12 vs e2L1=8 asymmetry at same
   len 0.5 — an artifact of stepped insert_at packing, not a fidelity concern; Rung 1 measures real fidelity.)
