# ── Line endings ─────────────────────────────────────────────────────────────
# Normalize all text to LF in the repository AND check out LF everywhere (2026-07-11; mirrors
# clusterio-docker 984a5fc). Checkout-native (autocrlf) was tried first and produced constant
# churn: every automated tool (agents, Node scripts) writes LF against a CRLF working tree,
# accumulating mixed-ending files and defeating exact-string edits. The index was already all-LF,
# so this changes CHECKOUT ONLY — no content rewrite. After pulling this, refresh your clone:
#   git config core.autocrlf false && git rm -rf --cached . -q && git reset --hard
# cmd/bat below still check out CRLF (later rules win).
* text=auto eol=lf

# Scripts executed inside Linux containers MUST stay LF even on Windows checkouts
# (a CRLF shebang or line-continuation breaks bash and Docker builds).
*.sh   text eol=lf
*.bash text eol=lf

# PowerShell checks out LF like everything else (2026-08-09). The index was already LF (text=auto
# normalizes on commit); only the CHECKOUT was CRLF, which is the last remaining source of the
# mixed-ending churn the rule above exists to stop — agents and Node tools write LF into those
# files. pwsh reads LF natively on Windows.
#   After pulling this:  git rm -rf --cached . -q && git reset --hard
*.ps1  text eol=lf
*.psm1 text eol=lf

# cmd.exe is genuinely line-ending sensitive (labels, goto). No such file exists today.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets — never normalize or diff as text.
*.zip   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary

# (The "CI Docker assets" block is gone. It forced LF on docker/ci/Dockerfile.factorio-baked and
# docker-compose.ci.yml so a CRLF checkout could not break `RUN ... \` line continuations. Both
# files were deleted with the Factorio bake — the rules named paths that no longer exist. The
# blanket `* text=auto eol=lf` above already covers any future Linux-consumed file.)
