# Clusterio Cluster Configuration Template
# Copy this file to .env and adjust the values for your environment

# Controller Ports
# Note: In Clusterio 2.0, WebSocket runs on the same port as HTTP
CONTROLLER_HTTP_PORT=8080

# Host 1 - Instance 1
HOST1_INSTANCE1_GAME_PORT=34197
HOST1_INSTANCE1_RCON_PORT=27015

# Host 2 - Instance 1
HOST2_INSTANCE1_GAME_PORT=34198
HOST2_INSTANCE1_RCON_PORT=27016

# Shared Configuration
RCON_PASSWORD=YourSecurePasswordHere

# Comma-separated Factorio admins (written into server-adminlist.json automatically)
FACTORIO_ADMINS=admin

# Auto-start instances after cluster initialization (true/false)
# When enabled, instances will start automatically when hosts connect
FACTORIO_AUTO_START=false

# Mod Pack Defaults
# Valid options: 2.0 (Space Age), 1.1 (legacy stable), 1.0 (original)
# Must match your Factorio version
MOD_PACK_FACTORIO_VERSION=2.0

# Factorio Portal Credentials (optional - only needed for downloading mods from portal)
# Get your token from https://factorio.com/profile
# Leave empty to skip mod portal downloads
FACTORIO_USERNAME=
FACTORIO_TOKEN=

# Plugin Management
# To enable/disable plugins, edit the PREINSTALLED_PLUGINS array in:
# docker/seed-data/scripts/clusterio-init.sh (lines 552-560)
# Plugin-specific config can be set via: clusterioctl config set plugin_name.setting_name value

# Seed Save File
# Specify which save file to load from docker/seed-data/saves/ (without .zip extension)
# If empty or not found, will use the newest save file in the directory
# Example: SEED_MAP_SAVE_NAME=my-world
SEED_MAP_SAVE_NAME=
